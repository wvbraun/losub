"use strict";

import React, { PropTypes } from "react";
import { Link } from "react-router";

const ClypTrackRow = ({ track }) => {
  return (
      <tr>
      </tr>
  );
};

ClypTrackRow.propTypes = {
};

export default ClypTrackRow;
